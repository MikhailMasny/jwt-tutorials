import React, { useContext } from 'react';
// import UserContext from '../contexts/UserContext'

const Content = () => {
    // const user = useContext(UserContext);
    // if (!user.accessToken) {
    //     // Redirect to login
    // }
    return <div>This is content for auth user.</div>
}


export default Content;
